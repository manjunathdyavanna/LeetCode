public boolean isPalindrome(int x) {
        if(x<0)
            return false;
        if(x==0)
            return true;
        int rx = 0;
        int temp=x;
        while(temp>0){
            rx = rx*10 + temp%10;
            temp /= 10;
        }
        return x==rx;
    }
